input {
    file {
        path => "/var/log/ufw.log"
        type => "iptables"
    }
}
# filter {
#
# }
output {
    elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "%{[@metadata][beat]}-%{[@metadata][version]}"
    }
#     stdout {
#         codec => rubydebug
#     }
}
