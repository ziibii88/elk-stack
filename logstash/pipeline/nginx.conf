input {
    file {
        path => "/var/log/nginx/*.log"
        type => "nginx"
    }
}
# filter {
#
# }
output {
    elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "logstash-nginx"
    }
#     stdout {
#         codec => rubydebug
#     }
}
